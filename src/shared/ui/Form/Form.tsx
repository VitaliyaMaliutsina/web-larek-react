import styles from "./form.module.scss";
import { Input } from "../Input/Input.tsx";

type Props = {};
export const Form = (props: Props) => {
  const {} = props;

  return (
    <form>
      <Input />
    </form>
  );
};
